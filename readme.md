# Angular 4 with Laravel 5.5 essentials
**By - Selvesan**

***
***

### Install Angular Js
First we need to install angular cli globally into our machine.  
`npm install -g @angular/cli`
  
Now Install Angular js Frontend Frameword  
`ng new ng4tutorial`
 
Run the angular server  
`ng serve --open` 